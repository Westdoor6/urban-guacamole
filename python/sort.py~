def swap(x,y):
    temp = x
    x = y
    y=x

def bubble_sort(list):
    for x in range(1,list):
        if list[x] < list[x-1]:
            swap(list[x], list[x-1])

x = [1,3,5,3,98,3,1,2,1]
bubble_sort(x)
